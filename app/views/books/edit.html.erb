<h1>Editing Book</h1>
<%= form_with model: @book do |f| %>
  <div class="field">
    <label>Title</label><br>
    <%= f.text_field :title %>
  </div>
  <div class="area">
    <label>Opinion</label><br>
    <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
  <% end %>